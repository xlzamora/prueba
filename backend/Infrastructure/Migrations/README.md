# EF Core Migrations

Este directorio contiene la carpeta de migraciones generadas por EF Core. Para generar la migración inicial:

```bash
dotnet ef migrations add InitialCreate --project ../Infrastructure --startup-project ../WebApi
```
